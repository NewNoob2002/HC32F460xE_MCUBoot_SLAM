safety_preflight:
  schema_version: 1
  timestamp_utc: "2026-08-28T08:56:58Z"
  operation: "cmsis-dap-peripheral-write/uart-open"
  authorization: "User requested continued UART HIL diagnosis after confirming DAPLink RX is connected to board TP2."
  target:
    board: "HC32F460xE; hardware revision 2"
    hardware_revision: "2"
    probe_or_adapter_serial: "Horco CMSIS-DAP 5844333732"
    transport: "CMSIS-DAP SWD at 4000 kHz; integrated UART 115200 8N1, 3.3 V TTL"
  artifact_or_stimulus: "Write the ASCII bytes UART_TEST\r\n once to initialized USART3 TDR at 0x40021004, with 20 ms between halfword writes, while capturing the DAPLink CDC UART."
  limits: "Write exactly 11 values in range 0x00..0xFF only to USART3 TDR 0x40021004. Do not reset, halt intentionally, write Flash, GPIO, clock, option-byte, protection, or other peripheral registers. Capture is bounded to 15 seconds. No automatic retry."
  recovery_plan: "The TDR stimulus is transient and requires no persistent rollback. Leave the target running. If no bytes are captured, stop active target tests and validate the DAPLink VCOM with a physical TX-RX loopback before changing firmware."
  approved: true
